# CLAUDE.md

Claude Code가 이 저장소에서 작업할 때 참고하는 프로젝트 지침서.
제품 기준은 **`docs/gdocs/`의 최신 버전 기획서** (현재 v0.10). 이 문서와 충돌하면 기획서가 우선이며, 충돌 발견 시 보고할 것.

## 기획서 동기화 (세션 시작 루틴)

- 세션 시작 시 `/sync-docs` 실행 (`.claude/commands/sync-docs.md`):
  `/Users/heewonjung/Documents/구글문서보관용_docs/myPoopAI_GoogleDoc` → `docs/gdocs/` 로
  **달라진 파일만** 복사. 원본 폴더가 비어 있으면 절대 덮어쓰기·삭제 금지.
- `docs/gdocs/` 는 **읽기 전용** — 수정은 항상 구글닥에서. 기획 변경 발견 시 코드와의 차이를 요약 보고하고, 적용은 승인 후 진행.

## 프로젝트 한 줄 요약

MyPoopAI(마이풉) — 고양이 캐릭터(푸푸·피피)와 **채팅으로 배변·배뇨를 기록**하는 AI 건강 기록 반응형 웹앱(PWA).
**채팅이 곧 앱이다.** 텍스트 기록이 기본 단위, 이미지는 SMS처럼 선택 첨부.
사용자는 자기 대변을 보고 싶어하지 않으므로 카메라·업로드 이미지는 **가리기(모자이크/스티커) 기본 ON**.
포지셔닝: 진단 도구가 아니라 "병원 가기 전 애매한 구간"의 기록·참고 도구. 핵심 타겟은 초보 부모 — **베이비 모드가 본체**.

## 기술 스택

| 구분 | 기술 |
| --- | --- |
| Frontend | React 18 + TypeScript + Vite (PWA) |
| 스타일 | Tailwind CSS (디자인 토큰 기반) |
| 카메라 | WebRTC + Canvas API (실시간 가리기, 첨부 플로우 내부) |
| AI | Claude API `claude-haiku-4-5` 기본 + 선별 승격 — 반드시 Cloud Functions 경유 |
| 인증 | Firebase Auth 4종: **익명(게스트) / Google / Apple / 이메일(인증 메일 필수)** — 카카오는 백로그 |
| DB / 스토리지 | Firebase Firestore / Storage |

## 명령어

```bash
npm run dev        # 개발 서버
npm run build      # 빌드
npm run lint       # 린트
npm run typecheck  # 타입 체크
```

작업을 마치면 최소한 `npm run typecheck`와 `npm run lint`는 통과시킬 것.

## 아키텍처 규칙

1. **탭 3개**: `src/pages/RecordPage`(기록 채팅 — 앱의 본체) / `HistoryPage` / `SettingsPage`.
   **카메라는 독립 화면이 아니다.** 채팅 입력창의 첨부(+)에서 열리는 시트/모달(`CameraSheet`)이며,
   열리는 순간부터 가림 ON이 디폴트다. 앨범 업로드도 화면 렌더 전에 Canvas에서 가림 처리한다.
2. **기록 카드 확정 원칙**: AI가 파싱한 기록은 채팅 안의 기록 카드로 제안되고(별점 0~5 · 0.5단위 수정 가능),
   **[기록 저장]을 누르기 전에는 저장하지 않는다.** 별점 수정 시 AI 코멘트를 갱신한다.
3. **게스트 = Firebase 익명 인증.** 기록은 게스트 포함 전부 서버(Firestore)에 저장한다.
   ~~LocalStorage 기록 저장~~ 정책은 폐기 — LocalStorage는 UI 설정·테마·온보딩 플래그 같은 기기 프리퍼런스만.
   가입 전환은 **반드시 `linkWithCredential`로 uid 유지**(기록 그대로 승계, 마이그레이션 없음).
4. **계정**: 4종 로그인 모두 + **전 계정(게스트 포함) 회원탈퇴 필수** (탈퇴 시 개인정보 즉시 삭제).
   **게스트는 히스토리 탭 잠금** — 기록은 자유, 열람은 가입 유도 포인트.
5. **Firebase 접근은 `src/lib/` 안에서만.** 컴포넌트에서 firestore를 직접 import 하지 않는다.
6. **AI 호출에 API 키를 클라이언트에 노출하지 않는다.** 반드시 Cloud Functions(callable + App Check) 경유.
7. **이미지 원본은 화면에 그대로 띄우지 않는다.** 가리기 처리된 결과만 렌더 (원본 열람은 재인증 플로우).
8. **BM 무료 쿼터** (서버 카운트, 자정 리셋): 텍스트 기록 **무제한** / 이미지 분석 **3회·일** / 일반대화 **3회·일**.
   greeting·별점 수정·7일 총평은 쿼터 미차감. 프리미엄 = 쿼터 무제한 + 동시 대화 + 광고 제거.
9. **캐릭터**: 푸푸(검은 고양이) · 피피(하얀 고양이) 중 **전역 1마리 선택**(`users.chatCharacter`), 자유 전환 + 인수인계 연출.
   첫만남은 `users.characterMeta.{pupu|pipi} = { hasMet, firstMetAt }`로 **서버가 판정**한다 (AI가 추측 금지).
   캐릭터 ≠ 모드 — 어떤 캐릭터든 성인/베이비 모두 담당.
10. **AI 기억의 소유권** (기획서 v0.10 §2): 프로필·배변 기록·패턴은 **캐릭터 공유**,
    일상 기억(entities)은 **캐릭터별 분리 보관** — `entities.character [pupu|pipi]`, 회수 시 현재 캐릭터 것만 주입.
    전환 시 삭제(초기화)가 아니라 보관이다. 인수인계로 넘어가는 건 건강 데이터뿐.
11. **기록 검색 연출**: tool use(기록_검색) 발동 시 캐릭터별 고정 문구 세트("잠깐 기록 좀 볼게냥 📖" 등)를
    중간 말풍선으로 즉시 표시한 뒤 본 답변을 렌더한다 — 검색 대기를 세계관으로 흡수.

## 폴더 규칙

- 새 최상위 폴더는 만들기 전에 물어볼 것. 기존 구조 재사용 우선.
- 대용량 이미지·디자인 원본은 repo에 넣지 않는다 (코드에 필요한 최소 에셋만 `src/assets/`).
- 파일 1개 100MB 금지, repo 1GB 이내.

## 디자인 토큰 (필수)

색상은 `design/tokens/colors.css`의 CSS 변수(시맨틱 토큰)만 사용한다. HEX 하드코딩 금지.
라이트는 `:root`, 다크는 `[data-theme="dark"]` 리매핑 — 컴포넌트는 토큰명만 참조한다.
다크 값은 기획서 v0.6 §6.1 시안표 기준 (WCAG 대비 검증 후 확정 예정).
**레트로 네온사인 v0.5 리디자인 검토 중** — 확정 시 colors.css 값만 교체된다. 토큰만 쓰면 컴포넌트는 안전하다.

| 토큰 | 라이트 | 다크 | 용도 |
| --- | --- | --- | --- |
| `bg.base` | `#F4F0E6` | `#221F1A` | 기본 배경 |
| `bg.card` | `#E9E3D4` | `#32291C` | 카드 · 냥이 말풍선 |
| `border.default` | `#D6CFBF` | `#45433E` | 구분선 · 기록 카드 테두리 |
| `text.primary` | `#3B362F` | `#E6E4DF` | 본문 |
| `text.secondary` | `#6E655A` | `#B3B0A8` | 서브 텍스트 · 아이콘 |
| `text.placeholder` | `#A79E8F` | `#918E86` | 플레이스홀더 |
| `text.heading` | `#221F1A` | `#F5F4F1` | 헤드라인 · 로고 (키컬러) |
| `accent.star` | `#E3C87E` | 유지 | 별점 채움 |
| `accent.badge` | `#C9A227` | 유지 | 하이라이트 · 뱃지 |
| `status.good` | `#7C7B45` | `#9C9A60` | 양호 (별점 4↑) ● |
| `status.caution` | `#C1704A` | 유지 | 주의 (2.5~3.5) ◐ |
| `status.warning` | `#8C4A3C` | `#A85B38` | 경고 (2↓) ▲ + '!' |
| `mode.adult` | `#7590A2` | `#9AAEBC` | 성인 모드 포인트 |
| `mode.baby` | `#BE8A8A` | `#D3A9A7` | 베이비 모드 포인트 |
| `action.link` | `#576F82` | `#9AAEBC` | 링크 · 액션 |
| `state.focus` | `#9D7F9B` | `#B99FB8` | 선택 · 포커스 |

채팅 말풍선 전용 토큰(`chat.*`)도 colors.css에 정의되어 있다 — 유저 말풍선은 모드별(성인 데님/베이비 로즈).

**색 사용 규칙** (v0.4에서 금지색 규정 폐지): 원색·네온 등 고채도 색의 **면(채움)** 사용 금지.
모든 색은 70~80년대 빛바랜 뮤트 톤 — 기본 화면은 모노톤, 컬러는 포인트·캐릭터·상태에 소량.
(네온사인 v0.5 확정 시 네온은 면이 아니라 **선** — 프레임·포커스·활성 글로우에만.)

## 반응형 — 기기별 역할 분담

| 구간 | 레이아웃 | 역할 |
| --- | --- | --- |
| Mobile (하단 탭바) | 1컬럼 | **풀 기능** — 채팅 기록, 카메라, 히스토리, 설정 |
| Tablet | 2단 히스토리 | 모바일과 동일 (세부 정책 오픈 이슈) |
| Desktop (좌측 내비) | max 1080~1200 | **조회 전용 뷰어** — 카메라·기록 입력 없음. 열람·인쇄·리포트 전용 |

현재 코드 브레이크포인트는 600/1024/1440 (기획서는 640/1024 — 통일은 추후 작업).
스페이싱 4/8/12/16/24/32/48/64 · 라운드 8(칩)/12(카드)/16(모달)/999(버튼) · 터치 타겟 최소 44px.

## 코드 컨벤션

- 컴포넌트 파일명 `PascalCase.tsx`, 훅 `useSomething.ts`, 유틸 `camelCase.ts`
- named export 우선 (`export function Foo`), default export는 페이지 컴포넌트만
- 주석과 UI 문구는 한국어. i18n 키는 영어 (`ko` / `en` 2개 로케일)
- 타입은 `src/types/index.ts`에 모아두고 재사용
- 시간: `createdAt`(serverTimestamp)과 `occurredAt`(발생 시각, 유저 수정 가능) 분리 — 표시는 기기 타임존
- 작은 단위로 작업하고 단계마다 커밋. `docs/05-dev-log.md` 갱신

## 하지 말 것

- 실제 사용자 사진 / 개인정보 / `.env` 커밋
- `docs/gdocs/` 안의 파일 수정 (읽기 전용 — 수정은 구글닥에서만)
- 디자인 토큰 무시하고 임의 색상 사용
- 원본(가리기 미적용) 이미지를 UI에 노출
- 가입 강제 — 게스트(익명 인증)로 기록 기능이 즉시 동작해야 한다 (단, 히스토리 잠금은 의도된 정책)
- [기록 저장] 확정 전에 기록을 저장하는 UX
- 아픈 똥에 드립 — 드립은 별점 양호 + 위험 신호 없음일 때만, emergency 시 전면 중단.
  드립은 AI 즉석 창작 금지 — drips 풀에서 선택만
- 요청 없는 대규모 리팩터링

## 문서

- **`docs/gdocs/` — 최신 기획서 (구글닥 자동 동기화, 읽기 전용, 최우선 기준)**
- `docs/00-product-overview.md` — 제품 개요 (구버전 — 기획서가 우선)
- `docs/01-design-system-v0.4.md` — 컬러 시스템
- `docs/02-screens.md` — 화면 스펙 (구버전 — 카메라 탭 시절)
- `docs/03-data-model.md` — Firestore 구조 (구버전 — 최신은 기획서 v0.8~0.10 스키마)
- `docs/04-roadmap.md` — 개발 단계
- `docs/05-dev-log.md` — 개발 히스토리. **작업을 마칠 때마다 항목을 추가할 것**
- `docs/06-ai-handoff.md` — 다른 AI와 공유하는 컨텍스트 + 기획서 요약. 구조가 바뀌면 "현재 상태" 갱신

